[Unit]
Description=A rule based proxy in Go.
After=network.target

[Service]
Type=simple
Restart=on-abort
ExecStart=/usr/bin/clash -f %E/clash/%i.yaml
StandardOutput=append:%E/clash/%i.log
StandardError=inherit

[Install]
WantedBy=default.target
